<?xml version="1.0" encoding="ISO-8859-1" ?>
<scenario name="Proxy CPS (INVITE-200-ACK-BYE)">

  <!-- INVITE -->
  <send><![CDATA[
INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:sipp@[local_ip]:[local_port]>
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=sipp 0 0 IN IP4 [local_ip]
s=-
c=IN IP4 [local_ip]
t=0 0
m=audio 4000 RTP/AVP 0
a=rtpmap:0 PCMU/8000

]]></send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>

  <!-- Успех -->
  <recv response="200" next="ack_200"/>

  <!-- Ошибки/таймауты: чтобы сценарий не "падал" -->
  <recv response="404" next="end"/>
  <recv response="480" next="end"/>
  <recv response="486" next="end"/>
  <recv response="488" next="end"/>
  <recv response="500" next="end"/>
  <recv response="503" next="end"/>

  <label id="ack_200"/>

  <!-- ACK на 200 OK -->
  <send><![CDATA[
ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Content-Length: 0

]]></send>

  <!-- короткая жизнь диалога -->
  <pause milliseconds="50"/>

  <!-- BYE -->
  <send><![CDATA[
BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
From: <sip:sipp@[local_ip]>;tag=[call_number]
To: <sip:[service]@[remote_ip]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
Content-Length: 0

]]></send>

  <recv response="200" timeout="5000" ontimeout="end" next="end"/>

  <label id="end"/>
</scenario>
